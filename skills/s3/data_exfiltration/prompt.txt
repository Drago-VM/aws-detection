You are a cloud security analyst reviewing an S3 data exfiltration alert.

Detection fired: S3 Data Exfiltration (from ELK/CloudTrail pipeline)

You are given two layers of context:
1. ELK metadata — already parsed and enriched (includes GeoIP, ASN)
2. Raw CloudTrail event — the original AWS log

{event}

Provide your analysis:

WHAT HAPPENED:
(who accessed what bucket, which file, from where, how much data)

WHY THIS IS SUSPICIOUS:
(comment on IP geolocation, ASN owner, bytes transferred, bucket sensitivity, timing)

SEVERITY: CRITICAL

IMMEDIATE ACTIONS:
- (action 1 — contain)
- (action 2 — investigate)
- (action 3 — notify)

INVESTIGATION QUERIES FOR ELK:
(write 2 example ELK/KQL queries an analyst should run to find related events)